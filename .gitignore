# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/

# Migration & Backups (production data)
backup/
migration/
private/
registry-v0-backup-*/

# Production entities (not examples)
# Keep examples/ but exclude real data
/entities/
/data/

# Reports with sensitive data
reports/*.json
!reports/.gitkeep

# OS
.DS_Store
Thumbs.db.venv_*

# =============================================================================
# SENSITIVE DATA — NEVER COMMIT
# =============================================================================
# Local-only infrastructure baselines, telemetry, and backups.
# These belong in ~/knowledge/knowledge-kernel/ (external dataset),
# not in the public code repository.

# Archive directory (moved sensitive data from repo)
_archive-sensitive/

# Temporary/backup files with real data
*.backup_*
*.md.bak*

# Hermes runtime telemetry (operational data)
.hermes/telemetry/

# Historical backups of telemetry
telemetry/archive/

# Personal/local-only migration snapshots
migration-snapshots/
