"""Inspector tools — Dataset-plane utilities (not part of the contract).

This package is independent of the `inspector.*` package by design: it
must be importable without forcing the Inspector contract into scope.

Modules:
    runs_diff     Compare two Inspector runs.
"""
